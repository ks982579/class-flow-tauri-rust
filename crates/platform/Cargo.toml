[package]
name = "platform"
version = "0.1.0"
edition = "2024"

[dependencies]
core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dependencies.tauri]
version = "2"
features = []
